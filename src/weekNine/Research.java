package weekNine;

public class Research {

	public static void main(String[] args) {

		/* Research 10 different datatypes used in MySQL and write how they are used and what makes them unique.
		 * 
		 * 1) INT: Used for standard integer types. Includes 4 bytes of storage (basically 4 characters). An INT will always
		 * 			be 4 bytes not matter what length is specified. The length indicated is supposed to specify how many
		 * 			characters to display when selecting data via the CLI.
		 * 
		 * 2) CHAR: Holds a fixed length string. Fixed size is indicated inside parenthesis but can store up to 255 characters.
		 * 
		 * 3) VARCHAR: Holds a variable length string. MAX size is indicated inside parenthesis but can store up to 255 characters.
		 * 
		 * 4) TEXT: Holds a string wit ha max length of 65,535 characters.
		 * 
		 * 5) BLOB: Binary Large Objects: Holds up to 65, 535 bytes of data.
		 * 
		 * 6) ENUM: Lets you enter a list of possible values. (For providing choices to pick from)
		 * 
		 * 7) FLOAT: small number with floating decimal points. Takes 2 parameters (max length, decimal points)
		 * 
		 * 8) DECIMAL: a DOUBLE stored as a string with a fixed decimal point. Takes 2 parameters (max size, decimal points)
		 * 
		 * 9) DATE: Date formatted: YYYY-MM-DD
		 * 
		 * 10) TIMESTAMP: holds the date and time including number of seconds
		 * 
		 */
	}

}
